# minerU
參考github repo : https://github.com/opendatalab/mineru

# pageindex 架構
參考github repo : https://github.com/VectifyAI/PageIndex
來展開tree 結構

# 向量資料庫 Qdrant 混合搜尋
參考官方網站: https://qdrant.tech/documentation/search/hybrid-queries/

# Qdrant 設定
http://your-qdrant-host:6333/

# embedding model 
使用lm studio 自架的模型
url : http://your-llm-host:1234
model : text-embedding-qwen3-embedding-0.6b
context length 4096

# llm model + agent agno框架
agent 框架使用agno
agent 請使用agno 框架來進行開發, 請參考agnoOS來進行
from agno.models.litellm import LiteLLMOpenAI
model = LiteLLMOpenAI(
    id=os.getenv("MODEL_ID", "qwen35-27b"),
    api_key=os.getenv("LITELLM_API_KEY"),
    base_url=os.getenv("LITELLM_BASE_URL", "http://localhost:4001/v1"),
)
model id 使用:  qwen35-27b
LITELLM_API_KEY: your-api-key


# agent 框架: agno
參考agno官網 : https://docs.agno.com/features/sdk
使用otel 追蹤: arize phoenix https://docs.agno.com/observability/arize

# phoenix 
url endpoint: https://your-phoenix-host
參考設定
DEFAULT_PHOENIX_ENDPOINT = "https://your-phoenix-host/v1/traces"
DEFAULT_PHOENIX_PROJECT = "a2ui-demo-agent"
DEFAULT_PHOENIX_PROTOCOL = "http/protobuf"
DEFAULT_PHOENIX_WORKING_DIR = Path(__file__).resolve().parent / ".phoenix"

# postgres db
# ── PostgreSQL 連線設定 ─────────────────────────────
# ⚠️ 必須使用 127.0.0.1，不要用 localhost（Windows DNS 解析延遲 135s+）
DB_URL = (
    f"postgresql+psycopg://"
    f"{os.getenv('DB_USER', 'postgres')}:"
    f"{os.getenv('DB_PASSWORD', 'change-me')}"
    f"@{os.getenv('DB_HOST', '127.0.0.1')}"
    f":{os.getenv('DB_PORT', '5433')}"
    f"/{os.getenv('DB_NAME', 'postgres')}"
)

# 虛擬環境安裝
一律使用uv 進行安裝

# 後端框架
python 3.12+
FastAPI

# 前端框架
React 19 , 蘋果簡潔風, 主體顏色淡藍色

